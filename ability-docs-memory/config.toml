# ability-docs-memory configuration
# Secrets go in secrets.toml (encrypted vault)

[broker.remote]
URL = "wss://broker.dadavidtseng.com/kadi"
NETWORKS = ["docs"]
MODE = "native"

[docs]
database = "agents_memory"
default_collection = "agents-docs"
embedding_model = "text-embedding-3-small"
extraction_model = "gpt-5-nano"
max_tokens = 500
base_url = "http://localhost:3333"
domain = "localhost"
embedding_transport = "api"
chat_transport = "api"
